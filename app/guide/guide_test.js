'use strict';

describe('tvGuide.guide module', function() {

  beforeEach(angular.mock.module('tvGuide.guide'));

  describe('guide controller', function(){

  	var GuideController = {};
  	beforeEach(angular.mock.inject(function(_GuideController_) {
  		GuideController = _GuideController_;
  	}))

  });
});